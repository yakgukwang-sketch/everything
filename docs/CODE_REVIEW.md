# everything 프로젝트 코드 리뷰 (2차)

> 1차 리뷰: 49/100 (2026-03-11) → **2차 리뷰: 62/100 (2026-03-17)**

## 종합 점수: 62/100 (+13)

### 주요 변경사항 (1차 → 2차)
- API 단일 파일 928줄 → **8개 라우트 모듈로 분리** (~2,500줄)
- 에이전트 13종 규칙 기반 → **12종 Gemini AI 기반** + 5종 배달 에이전트
- 크롤러 1종(뽐뿌) → **2종(뽐뿌 + 디시인사이드 63개 갤러리)** + 대량 크롤링
- 프론트엔드 3페이지 → **6페이지** + DealFeed/DeliveryFlow 컴포넌트 분리
- 공유 타입 추출 (`shared.ts`, `types.ts`)
- 배달 시스템 신규 (주문→에이전트 입찰→기사 입찰→배달→리뷰)
- comments DB/API 추가

---

## 1. 프로젝트 구조 — 8/10 (유지)

**좋은 점:**
- 모노레포 3분할 (api/web/data) 깔끔
- API 라우트 모듈 분리 (8개 파일)
- 프론트엔드 공유 코드 추출 (`shared.ts`)
- 크롤러 base/ppomppu/dcinside 분리
- docs/ 문서 정리

**감점 (-2):**
- CI/CD 없음, 린팅 설정 없음
- `web/src/app/page.tsx` 680줄 — 여전히 비대
- 크롤러 `data/` 내 데이터 파일(JSON)과 소스 코드 혼재

**정량 데이터:**
| 영역 | 파일 수 | 총 LOC |
|------|---------|--------|
| API (routes) | 8개 | ~1,800 |
| API (strategies) | 2개 | ~280 |
| API (types/index) | 2개 | ~180 |
| Web (pages) | 6개 | ~2,200 |
| Web (components) | 2개 | ~290 |
| Web (lib) | 2개 | ~200 |
| Data (crawlers) | 4개 | ~700 |
| Data (scripts) | 2개 | ~250 |
| **합계** | **~28개 소스** | **~5,900** |

---

## 2. API 설계 — 8/10 (+1)

**좋은 점:**
- 모듈화된 Hono 라우트 (deals, agents, delivery, drivers, stores, comments, chat, discovery, developers)
- RESTful 구조: 25+ 엔드포인트
- SQL 파라미터 바인딩 100%
- CORS origin 검증 (도메인 화이트리스트)
- 크롤러용 엔드포인트 인증 (Bearer token)
- `AgentCore<TContext, TItem>` 제네릭 인터페이스

**감점 (-2):**
- POST 요청 입력 검증 불균일 (deals/submit은 검증, comments는 최소)
- 응답 구조 불일치 (`{ data, meta }` vs `{ data, total }`)
- API 버저닝 없음

**정량 데이터:**
| 메트릭 | 값 |
|--------|-----|
| 엔드포인트 | 25+ (GET 15, POST 10, PATCH 1) |
| 라우트 모듈 | 9개 |
| SQL 쿼리 | 40+ (전부 파라미터 바인딩) |
| POST 검증 비율 | 7/10 양호 |
| 인증 적용 | 크롤러 엔드포인트 3개 |

---

## 3. 보안 — 3/10 (+2)

**개선된 점:**
- CORS 도메인 화이트리스트 적용 (pages.dev + localhost:3000)
- 크롤러 업로드 엔드포인트 Bearer 인증
- API 키 `wrangler secret put`으로 관리 (toml에서 제거)

**여전히 미흡:**
- 공개 POST 엔드포인트 인증 없음 (deals/submit, drivers/register, developers/register, reviews)
- CSRF 토큰 없음
- Rate Limiting 없음
- XSS: 크롤링 데이터 새니타이징 부족 (sanitizeUrl은 있으나 title/description은 미처리)
- 개발자 등록 이메일 검증 너무 허술
- API 키 생성 `crypto.randomUUID()` — 예측 불가능하나 포맷이 단순

**정량 데이터:**
| 보안 항목 | 상태 | 변화 |
|-----------|------|------|
| 시크릿 관리 | wrangler secret | 개선 |
| CORS | 도메인 제한 | 개선 |
| 크롤러 인증 | Bearer token | 신규 |
| 공개 엔드포인트 인증 | 없음 | 미변 |
| CSRF | 없음 | 미변 |
| Rate Limiting | 없음 | 미변 |
| XSS 방어 | URL만 | 미흡 |

---

## 4. 타입 안전성 — 6/10 (+2)

**개선된 점:**
- `types.ts`에 DB Row 타입 7개 정의 (DealRow, AgentRow, StoreRow, CommentRow 등)
- `AgentCore<TContext, TItem>` 제네릭 인터페이스
- `shared.ts`에 프론트엔드 타입 통합
- tsconfig strict 모드 (api, web 모두)

**감점 (-4):**
- `as` 타입 캐스팅 다수 (D1 결과, JSON 파싱)
- API 응답 런타임 검증 없음 (Zod 등 미사용)
- 매직 넘버 ~30개 (점수 가중치, 가격 기본값, 딜레이 등)
- `any` 사용 감소했으나 여전히 10곳+

**정량 데이터:**
| 파일 | 인터페이스 | `any`/`as` | 매직넘버 |
|------|-----------|-----------|---------|
| types.ts | 7개 | 0 | 0 |
| routes/*.ts | 0개 | ~15 | ~20 |
| shared.ts | 6개 | 0 | 0 |
| page.tsx | 0개 | ~3 | ~5 |

---

## 5. 에러 처리 — 4/10 (+1)

**개선된 점:**
- 크롤러 upload 실패 시 로컬 JSON 백업
- Gemini 호출 실패 시 fallback 응답
- 대량 크롤링 중단 후 재개 가능 (_progress.json)

**감점 (-6):**
- 프론트엔드: `console.error`만 — 사용자 피드백 부족
- React Error Boundary 0개
- 에이전트 리뷰 레이스 컨디션 (SELECT → UPDATE 사이 정합성)
- 배달 플로우 상태 전환에 race condition 가능
- 크롤러: `except Exception` 포괄적 — 에러 유형 미구분

**정량 데이터:**
| 영역 | try-catch/except | 사용자 피드백 | Fallback |
|------|-----------------|-------------|----------|
| API | 12개 | N/A | Gemini 1곳 |
| Web | 6개 | 2곳 | 0 |
| Data | 15개 | N/A | JSON 백업 |

---

## 6. 테스트 — 0/10 (유지)

- 자동화된 테스트: **0개**
- 테스트 프레임워크: **미설치**
- CI/CD: **없음**

변경 없음. 프로젝트 규모가 커졌으므로 더 시급해짐.

---

## 7. 프론트엔드 품질 — 6/10 (+1)

**개선된 점:**
- 공유 타입/상수/유틸 추출 (`shared.ts`, `delivery-utils.ts`)
- 컴포넌트 분리 (`DealFeed.tsx`, `DeliveryFlow.tsx`)
- 6개 페이지 기능 분리 (메인, 에이전트, 배달, 기사, 딜헌터, 상품등록)
- 배달 플로우 다단계 UI (주문→에이전트 입찰→기사→배달→리뷰)

**감점 (-4):**
- `page.tsx` 680줄 — 여전히 비대 (컴포넌트 추가 분리 필요)
- `useState` 20개+ (page.tsx에만 10+) — 상태 관리 복잡
- 접근성: ARIA 라벨 0개, 키보드 내비게이션 미지원
- 인라인 스타일 다수
- 예산 파싱 "2~3만원" 형태 미지원

**정량 데이터:**
| 메트릭 | 값 |
|--------|-----|
| 페이지 수 | 6개 |
| 컴포넌트 수 | 2개 |
| 공유 라이브러리 | 2개 (shared.ts, delivery-utils.ts) |
| useState | 20+ |
| fetch 호출 | 12+ |

---

## 8. 크롤러 품질 — 7/10 (+3)

**개선된 점:**
- Deal + Comment dataclass로 일관된 모델
- 뽐뿌 전체 필드 수집 (추천/비추/조회/댓글수/작성자/가격)
- 디시인사이드 멀티 갤러리 크롤러 (`crawl_gallery()` 범용 함수)
- 63개 쇼핑 갤러리 자동 식별 + 대량 수집
- 대량 크롤링 중단/재개 시스템 (`_progress.json`)
- 댓글 수집: 뽐뿌 HTML 파싱 + 디시 JSON API
- upload_deals/comments/stores 3종 업로드 함수

**감점 (-3):**
- 재시도 로직 없음 — 실패 시 건너뜀
- 로깅: `print()` — logging 모듈 미사용
- 인코딩 `euc-kr` + `errors="replace"` — 데이터 일부 유실 가능
- DOM 파싱 사이트 구조 의존 (사이트 변경 시 깨짐)
- 디시 댓글 API `comments: null` 처리 (이전 버그 수정됨)

**정량 데이터:**
| 파일 | LOC | 크롤링 대상 | 댓글 지원 |
|------|-----|-----------|----------|
| base.py | 110 | - | - |
| ppomppu.py | 230 | 뽐뿌 200페이지 | div.comment_wrapper |
| dcinside.py | 200 | 디시 63개 갤러리 | JSON API |
| bulk_crawl.py | 200 | 통합 대량 수집 | - |
| main.py | 52 | 일반 실행 | - |

---

## 9. CSS/UI — 6/10 (유지)

**좋은 점:**
- 반응형 레이아웃
- 딜/에이전트/배달 카드 시각적 완성도 양호
- 로딩 애니메이션
- 이미지 lazy loading + 에러 핸들링

**감점 (-4):**
- CSS 변수 미사용 (색상 하드코딩)
- globals.css 단일 파일 — 컴포넌트별 분리 없음
- 포커스 스타일 미정의
- 미사용 CSS 클래스 존재

---

## 10. 문서화 — 7/10 (유지)

**좋은 점:**
- OVERVIEW.md, AGENT_STRATEGY.md, API_KEYS_GUIDE.md 실용적
- CODE_REVIEW.md로 코드 품질 추적
- MEMORY.md로 프로젝트 컨텍스트 유지

**감점 (-3):**
- API 엔드포인트 문서 없음 (요청/응답 스키마)
- 코드 내 주석 부족 (전략 함수, 점수 알고리즘)
- 설치/개발 가이드 없음

---

## 4차원 분석

### 축 1: 기능 완성도

| 기능 | 상태 | 비고 |
|------|------|------|
| 뽐뿌 크롤링 | 동작 | 200페이지, 전체 필드(추천/비추/조회/댓글수) |
| 디시인사이드 크롤링 | 동작 | 63개 쇼핑 갤러리, 멀티 갤러리 |
| 댓글 수집 | 동작 | 뽐뿌 HTML + 디시 JSON API |
| 대량 크롤링 | 동작 | 중단/재개, 갤러리별 JSON 저장 |
| 딜 검색/필터 | 동작 | 키워드, 소스, 카테고리 |
| AI 에이전트 12종 | 동작 | Gemini 기반 병렬 분석 |
| 대화형 사전질문 | 동작 | Gemini 채팅 |
| 배달 시스템 | 동작 | 주문→에이전트 입찰→기사→배달→리뷰 |
| 에이전트 등록/랭킹 | 동작 | 개발자 등록 + API 키 발급 |
| 기사 대시보드 | 동작 | 등록, 잡 목록, 수락 |
| 상품 등록 | 동작 | 사업자 직접 등록 |

**점수: 9/10** — MVP 기능 거의 완성. 배달+크롤링 대폭 확장.

### 축 2: 코드 견고성

| 지표 | 값 | 판정 |
|------|-----|------|
| 테스트 커버리지 | 0% | 위험 |
| 에러 바운더리 | 0개 | 위험 |
| 입력 검증 비율 | 70% (7/10 POST) | 보통 |
| SQL Injection 방어 | 100% | 양호 |
| 타입 안전성 | `any` ~10곳 | 보통 |
| 레이스 컨디션 | 2곳 확인 | 미흡 |
| 재시도 로직 | 0곳 | 미흡 |
| Fallback | Gemini 1곳 | 보통 |

**점수: 4/10** — 모듈화 개선으로 소폭 상승, 테스트 부재 여전.

### 축 3: 보안 성숙도

| 보안 항목 | 상태 | 변화 |
|-----------|------|------|
| 시크릿 관리 | wrangler secret | 개선 |
| CORS | 도메인 제한 | 개선 |
| 인증/인가 | 크롤러만 | 부분 개선 |
| XSS 방어 | URL만 | 미흡 |
| CSRF 방어 | 없음 | 미변 |
| Rate Limiting | 없음 | 미변 |

**점수: 3/10** — 기본적인 조치 시작, 공개 엔드포인트 여전히 취약.

### 축 4: 유지보수성

| 지표 | 값 | 판정 | 변화 |
|------|-----|------|------|
| 코드 중복 | ~50줄 (대폭 감소) | 보통 | 개선 |
| 모듈화 | API 8개 라우트 분리 | 양호 | 대폭 개선 |
| 매직 넘버 | ~30개 | 보통 | 소폭 개선 |
| 공유 코드 | types.ts, shared.ts | 양호 | 신규 |
| 린팅 설정 | 없음 | 나쁨 | 미변 |
| CI/CD | 없음 | 나쁨 | 미변 |
| 상태 관리 | useState 20+개 | 보통 | 미변 |

**점수: 5/10** — 모듈화/공유 코드 대폭 개선.

---

## 최종 점수 산출

| # | 항목 | 1차 | 2차 | 가중치 | 가중 점수 |
|---|------|-----|-----|--------|-----------|
| 1 | 프로젝트 구조 | 8 | 8 | x1.0 | 8.0 |
| 2 | API 설계 | 7 | 8 | x1.0 | 8.0 |
| 3 | 보안 | 1 | 3 | x1.5 | 4.5 |
| 4 | 타입 안전성 | 4 | 6 | x1.0 | 6.0 |
| 5 | 에러 처리 | 3 | 4 | x1.0 | 4.0 |
| 6 | 테스트 | 0 | 0 | x1.5 | 0.0 |
| 7 | 프론트엔드 품질 | 5 | 6 | x1.0 | 6.0 |
| 8 | 크롤러 품질 | 4 | 7 | x1.0 | 7.0 |
| 9 | CSS/UI | 6 | 6 | x0.5 | 3.0 |
| 10 | 문서화 | 7 | 7 | x0.5 | 3.5 |
| | | | | **합계** | **50.0 / 80** |

**최종: 50.0 / 80 = 62.5 → 62/100**

### 다각 분석 요약

| 축 | 1차 | 2차 | 한 줄 |
|----|-----|-----|-------|
| 기능 완성도 | 8/10 | 9/10 | 배달+크롤링 대폭 확장, 거의 완성 |
| 코드 견고성 | 3/10 | 4/10 | 모듈화 개선, 테스트 부재 여전 |
| 보안 성숙도 | 1/10 | 3/10 | 기본 조치 시작, 공개 엔드포인트 취약 |
| 유지보수성 | 3/10 | 5/10 | 모듈 분리+공유 코드로 대폭 개선 |

---

## 1차 대비 개선 요약

| 항목 | 1차 | 2차 | 변화 |
|------|-----|-----|------|
| API 모듈화 | 1파일 928줄 | 12파일 ~2,500줄 | 대폭 개선 |
| 에이전트 | 13종 규칙 기반 | 12종 Gemini AI + 5종 배달 | 대폭 개선 |
| 타입 정의 | 2개 중복 | 7개 DB Row + 6개 FE 공유 | 개선 |
| CORS | 완전 개방 | 도메인 화이트리스트 | 개선 |
| API 인증 | 없음 | 크롤러 Bearer token | 부분 개선 |
| 크롤러 | 뽐뿌 1종, 기본 필드 | 뽐뿌+디시 63갤러리, 전체 필드+댓글 | 대폭 개선 |
| 프론트 페이지 | 3개 | 6개 + 2 컴포넌트 | 개선 |
| 배달 시스템 | 없음 | 풀 플로우 (주문~리뷰) | 신규 |

---

## 개선 우선순위

### 즉시 (보안)
1. 공개 POST 엔드포인트 Rate Limiting
2. XSS 새니타이징 (크롤링 데이터 title/description)
3. CSRF 토큰 (submit, register)

### 1주 (안정성)
4. 테스트 프레임워크 도입 (Vitest + pytest)
5. 핵심 경로 테스트 (에이전트 쿼리, 배달 플로우, 크롤러 파싱)
6. React Error Boundary
7. `page.tsx` 680줄 컴포넌트 분리

### 1개월 (품질)
8. 입력 검증 라이브러리 (Zod)
9. CI/CD 파이프라인
10. 린팅 설정 (ESLint + Prettier + ruff)
11. API 문서 (OpenAPI)
12. 상태 관리 리팩터링 (useReducer 또는 Zustand)

### 다음 분기 (성장)
13. 접근성 개선 (ARIA, 키보드 내비게이션)
14. CSS 모듈화 (CSS Modules 또는 Tailwind)
15. 캐싱 레이어 (Cloudflare KV)
16. 크롤러 재시도 로직 + logging 모듈
