# everything 프로젝트 코드 리뷰

## 종합 점수: 49/100

### 채점 기준

10개 항목을 각 10점 만점으로 평가. 실제 코드 메트릭 기반 정량 분석.

---

## 1. 프로젝트 구조 — 8/10

**좋은 점:**
- 모노레포 4분할 (api/web/data/app) 깔끔
- 각 레이어별 독립 package.json, tsconfig.json
- docs/ 분리, agents/ 분리

**감점 (-2):**
- API가 단일 파일 928줄 (모듈 분리 없음)
- 프론트엔드 공유 코드 추출 없음 (타입, 상수, 유틸이 각 파일에 중복)
- CLAUDE.md 없음, CI/CD 없음, 린팅 설정 없음

**정량 데이터:**
| 영역 | 파일 수 | 총 LOC |
|------|---------|--------|
| API | 1 (index.ts) | 928 |
| Web | 4 (page/search/submit/css) | 2,725 |
| Data | 4 (main/ppomppu/base/crawl_all) | 344 |
| **합계** | **9개 소스 파일** | **3,997** |

---

## 2. API 설계 — 7/10

**좋은 점:**
- RESTful 구조: 17개 엔드포인트 (11 GET, 6 POST)
- 에이전트 전략 패턴: 13종 독립 전략 함수
- HTTP 상태 코드 적절 (400, 404, 500, 502 구분)
- SQL 파라미터 바인딩 100% — SQL Injection 방어

**감점 (-3):**
- POST /api/deals 입력 검증 없음 (배열 길이 무제한)
- POST /api/agents/query 최소 검증만 (query 존재 여부)
- 단일 파일에 엔드포인트 + 전략 + DB 로직 혼재
- 응답 구조 불일치 (data vs results vs 직접 반환)

**정량 데이터:**
| 메트릭 | 값 |
|--------|-----|
| 엔드포인트 | 17개 (11 GET + 6 POST) |
| SQL 쿼리 | 23개 (전부 파라미터 바인딩) |
| 에이전트 전략 | 13종 |
| POST 검증 비율 | 4/6 양호, 1 미흡, 1 최소 |
| try-catch | 4개 |

---

## 3. 보안 — 1/10

**CRITICAL:**
1. **API 키 평문 노출** — `wrangler.toml`에 Gemini API 키 하드코딩, git에 커밋됨
2. **인증 없음** — 모든 POST 엔드포인트 (딜 등록, 에이전트 등록, 리뷰) 누구나 호출 가능
3. **CORS 완전 개방** — `cors()` 파라미터 없이 모든 origin 허용
4. **XSS 취약점** — 외부 크롤링 데이터 (title, url, description) 새니타이징 없이 렌더링

**추가 이슈:**
- CSRF 토큰 없음 (submit 페이지)
- Rate Limiting 없음
- ID 생성: `Date.now()` + 약한 랜덤 (충돌 가능)

**감점 근거:** 4개 CRITICAL 중 해결된 것 없음. 프로덕션 노출 시 즉시 악용 가능.

---

## 4. 타입 안전성 — 4/10

**좋은 점:**
- tsconfig strict 모드 활성화 (api, web 모두)
- Deal, AgentResponse 타입 정의 존재

**감점 (-6):**
- API: `any` 14곳 (`as any` 캐스팅, catch 블록, 전략 함수 내부)
- Web: `any` 5곳 (deals 배열, parsed 객체, deal 매핑)
- API 응답 구조 검증 없음 — `data.data`, `data.success` 존재 가정
- 매직 넘버 ~45개 (신뢰도 계산 상수, 가격 범위, 시간 임계값 등)

**정량 데이터:**
| 파일 | `any` 사용 | 인터페이스 정의 |
|------|-----------|----------------|
| api/src/index.ts | 14회 | 0개 |
| web/page.tsx | 3회 | 2개 (Deal, AgentResponse) |
| web/search/page.tsx | 2회 | 2개 (중복 정의) |
| web/submit/page.tsx | 0회 | 0개 |

---

## 5. 에러 처리 — 3/10

**좋은 점:**
- API에서 HTTP 상태 코드 구분 (400/404/500/502)
- 크롤러 upload 실패 시 로컬 JSON 백업

**감점 (-7):**
- **API**: try-catch 4개뿐. 벌크 INSERT 실패 시 무시, 에이전트 쿼리 DB 작업 try-catch 없음
- **프론트**: `console.error`만 — 사용자 피드백 없음 (submit 페이지만 예외)
- **크롤러**: `print(f"Error: {e}")` 후 continue/break — 에러 유형 구분 없음
- React Error Boundary 0개
- 에이전트 리뷰 레이스 컨디션 (SELECT → UPDATE 사이 정합성 깨짐)

**정량 데이터:**
| 영역 | try-catch/except | 사용자 피드백 | Error Boundary |
|------|-----------------|-------------|----------------|
| API | 4개 | N/A | N/A |
| Web | 3개 catch | 1곳 (submit) | 0개 |
| Data | 5개 except | N/A | N/A |

---

## 6. 테스트 — 0/10

- API 엔드포인트 테스트: **0개**
- 크롤러 파싱 테스트: **0개**
- 프론트엔드 컴포넌트 테스트: **0개**
- E2E 테스트: **0개**
- 테스트 프레임워크 미설치 (Jest, Vitest, pytest 등)
- CI/CD 파이프라인 없음

`api/test_*.json` 파일은 수동 테스트 데이터일 뿐, 자동화된 테스트가 아님.

---

## 7. 프론트엔드 품질 — 5/10

**좋은 점:**
- 모든 비동기 작업에 로딩 상태 존재 (feedLoading, chatLoading, agentLoading, submitting)
- 이미지 에러 핸들링 (onError로 숨김)
- 반응형 CSS 기본 지원

**감점 (-5):**
- **코드 중복 ~150줄**: Deal/AgentResponse 타입, SOURCE_NAMES, AGENT_ICONS, AGENT_INTROS, formatPrice(), 로고 JSX가 2~3개 파일에 복사됨
- **useState 18개** (page.tsx 9, search 6, submit 3) — 상태 관리 복잡
- **접근성 0점**: ARIA 라벨 0개, role 속성 0개, focus-visible 없음, 키보드 내비게이션 미지원
- 인라인 스타일 51곳 (submit/page.tsx는 54%가 인라인)
- 모바일 브레이크포인트 2개뿐 (768px, 480px) — 600px, 1024px 범위 없음

**정량 데이터:**
| 메트릭 | page.tsx | search | submit | globals.css |
|--------|----------|--------|--------|-------------|
| LOC | 592 | 343 | 325 | 1,465 |
| useState | 9 | 6 | 3 | - |
| fetch 호출 | 3 | 2 | 1 | - |
| 인라인 스타일 | 15 | 14 | 22 | - |
| className | 87 | 44 | 19 | - |

---

## 8. 크롤러 품질 — 4/10

**좋은 점:**
- Deal dataclass로 일관된 모델 (base.py)
- 페이지 내 중복 제거 (seen set)
- crawl_all.py에 에러 카운터 + 점진적 백오프 (10에러 후 delay 증가)

**감점 (-6):**
- **ppomppu.py와 crawl_all.py 파싱 로직 70% 중복** — 동일한 셀렉터, 정규식, URL 조립을 각각 구현
- **crawl_all.py는 Deal dataclass 미사용** — plain dict, 키 이름도 다름
- 재시도 로직 없음 — 실패한 페이지 건너뜀
- 로깅: 전부 `print()` — logging 모듈 미사용
- 타입 힌트: ppomppu.py만 부분 적용 (~40%), crawl_all.py 0%
- 인코딩 `euc-kr` 하드코딩 + `errors="replace"` — 데이터 무결성 훼손
- 크로스 페이지 중복 제거 없음 — 같은 게시글 다른 페이지에서 재수집 가능

**정량 데이터:**
| 파일 | LOC | try-except | 타입 힌트 | 재시도 |
|------|-----|-----------|----------|--------|
| main.py | 39 | 1 | 없음 | 없음 |
| ppomppu.py | 136 | 2 | 40% | 없음 |
| base.py | 45 | 1 | 50% | 없음 |
| crawl_all.py | 124 | 1 | 없음 | 없음 |

---

## 9. CSS/UI — 6/10

**좋은 점:**
- 반응형 레이아웃 기본 구현
- 딜 카드, 에이전트 카드 시각적 완성도 양호
- 로딩 애니메이션 (typing dots, spinner)
- 이미지 lazy loading (page.tsx에서)

**감점 (-4):**
- CSS 변수 미사용 — `#4285f4`, `#ea4335`, `#202124` 등 색상 하드코딩 산재
- 미사용 CSS 클래스: `.categories`, `.category-tag`, `.search-buttons`, `.search-btn` 등
- 포커스 스타일 미정의 (focus-visible 없음)
- 색상 대비 미검증 (placeholder #9aa0a6)
- globals.css 1,465줄 단일 파일 — 컴포넌트별 분리 없음

---

## 10. 문서화 — 7/10

**좋은 점:**
- 비전/전략 문서 충실 (README.md, AGENT_STRATEGY.md)
- 프로젝트 구조 개요 명확 (OVERVIEW.md)
- 크롤링 가이드 실용적 (API_KEYS_GUIDE.md)
- 에이전트 분류 체계 정리 (agents/README.md)

**감점 (-3):**
- API 문서 없음 (요청/응답 스키마, 에러 코드 등)
- 코드 내 주석 부재 — 전략 함수 13개에 주석 0개
- 설치/개발 가이드 없음 (로컬 환경 세팅 방법)
- CLAUDE.md 없음

---

## 4차원 분석 — 다각적 평가

### 축 1: 기능 완성도 (What works)

| 기능 | 상태 | 비고 |
|------|------|------|
| 뽐뿌 크롤링 | 동작 | 37개 쇼핑몰 데이터 수집 |
| 딜 검색/필터 | 동작 | 키워드, 소스, 카테고리 |
| 핫딜 랭킹 | 동작 | 추천수/최신성/키워드 기반 점수 |
| 에이전트 13종 경쟁 | 동작 | 규칙 기반 전략 엔진 |
| 대화형 사전질문 | 동작 | Gemini API 연동 |
| 에이전트 평가 | 동작 | 별점 + 리뷰 |
| 트렌드 분석 | 동작 | 카테고리/소스/검색어 |
| 딜 제보 | 동작 | submit 페이지 |

**점수: 8/10** — MVP 기능은 거의 다 구현됨. 에이전트가 규칙 기반인 점만 미완.

### 축 2: 코드 견고성 (How reliable)

| 지표 | 값 | 판정 |
|------|-----|------|
| 테스트 커버리지 | 0% | 위험 |
| 에러 바운더리 | 0개 | 위험 |
| 입력 검증 비율 | 67% (4/6 POST) | 보통 |
| SQL Injection 방어 | 100% | 양호 |
| 타입 안전성 | `any` 19곳 | 미흡 |
| 레이스 컨디션 | 1곳 확인 | 미흡 |
| 재시도 로직 | 0곳 | 미흡 |

**점수: 3/10** — 동작은 하지만, 예외 상황에서 깨질 가능성 높음.

### 축 3: 보안 성숙도 (How secure)

| 보안 항목 | 상태 |
|-----------|------|
| 시크릿 관리 | API 키 평문 커밋 |
| 인증/인가 | 없음 |
| CORS | 완전 개방 |
| XSS 방어 | 없음 |
| CSRF 방어 | 없음 |
| Rate Limiting | 없음 |
| 입력 새니타이징 | 없음 |

**점수: 1/10** — 프로덕션 배포 상태에서 7가지 보안 항목 전부 미충족.

### 축 4: 유지보수성 (How maintainable)

| 지표 | 값 | 판정 |
|------|-----|------|
| 코드 중복 | ~150줄 (프론트) + 70% (크롤러) | 나쁨 |
| 모듈화 | API 단일 파일, 공유 코드 없음 | 나쁨 |
| 매직 넘버 | ~45개 (API) | 나쁨 |
| 린팅 설정 | 없음 | 나쁨 |
| CI/CD | 없음 | 나쁨 |
| 상태 관리 | useState 18개 분산 | 보통 |
| 문서화 | 전략 문서 양호, 코드 문서 부족 | 보통 |

**점수: 3/10** — 현재 규모에서는 관리 가능하나, 확장 시 기술 부채 급증.

---

## 최종 점수 산출

### 항목별 점수

| # | 항목 | 점수 | 가중치 | 가중 점수 |
|---|------|------|--------|-----------|
| 1 | 프로젝트 구조 | 8/10 | x1.0 | 8.0 |
| 2 | API 설계 | 7/10 | x1.0 | 7.0 |
| 3 | 보안 | 1/10 | x1.5 | 1.5 |
| 4 | 타입 안전성 | 4/10 | x1.0 | 4.0 |
| 5 | 에러 처리 | 3/10 | x1.0 | 3.0 |
| 6 | 테스트 | 0/10 | x1.5 | 0.0 |
| 7 | 프론트엔드 품질 | 5/10 | x1.0 | 5.0 |
| 8 | 크롤러 품질 | 4/10 | x1.0 | 4.0 |
| 9 | CSS/UI | 6/10 | x0.5 | 3.0 |
| 10 | 문서화 | 7/10 | x0.5 | 3.5 |
| | | | **합계** | **39.0 / 80** |

**최종: 39.0 / 80 = 48.8 → 49/100**

> 보안(x1.5)과 테스트(x1.5)에 높은 가중치를 부여. 프로덕션 서비스에서 이 두 항목이 치명적이기 때문.

### 다각 분석 요약

| 축 | 점수 | 한 줄 |
|----|------|-------|
| 기능 완성도 | 8/10 | MVP 기능 거의 완성 |
| 코드 견고성 | 3/10 | 테스트 0%, 에러 처리 미흡 |
| 보안 성숙도 | 1/10 | CRITICAL 4건 미해결 |
| 유지보수성 | 3/10 | 중복 많고 모듈화 부족 |

---

## 파일별 이슈 수

| 파일 | CRITICAL | HIGH | MEDIUM | 총 |
|------|----------|------|--------|-----|
| `api/wrangler.toml` | 1 | - | - | 1 |
| `api/src/index.ts` | 2 | 4 | 5 | 11 |
| `web/src/app/page.tsx` | 1 | 2 | 4 | 7 |
| `web/src/app/search/page.tsx` | - | 2 | 3 | 5 |
| `web/src/app/submit/page.tsx` | - | - | 2 | 2 |
| `web/src/app/globals.css` | - | - | 3 | 3 |
| `data/crawlers/ppomppu.py` | - | 2 | 3 | 5 |
| `data/crawl_all.py` | - | 1 | 3 | 4 |
| `data/crawlers/base.py` | - | 1 | 2 | 3 |
| `data/main.py` | - | 1 | 1 | 2 |
| **합계** | **4** | **13** | **26** | **43** |

---

## 잘한 점

1. 모노레포 구조 깔끔 (api/web/data/agents/docs 분리)
2. 에이전트 13종 전략 패턴 — 차별화된 영업사원 컨셉
3. SQL 파라미터 바인딩 100% — SQL Injection 완벽 방어
4. 반응형 CSS + 로딩 상태 구현
5. 크롤러 Deal dataclass로 일관된 모델
6. HOT 점수 알고리즘 (추천수 x10 + 최신성 + 키워드 보너스)
7. Gemini 대화형 니즈 파악 → 에이전트 경쟁 파이프라인
8. 전략/비전 문서 충실
9. 딜 제보 페이지 추가
10. tsconfig strict 모드 활성화

---

## 개선 우선순위

### 즉시 (보안)
1. API 키 폐기 → `wrangler secret put` 전환
2. CORS 도메인 제한
3. 쓰기 엔드포인트 인증 추가
4. XSS 새니타이징

### 1주 (안정성)
5. 입력값 검증 강화
6. TypeScript 인터페이스 정의 (`any` 제거)
7. 프론트엔드 공유 코드 추출 (types.ts, constants.ts, utils.ts)
8. 크롤러 코드 통합 (ppomppu.py + crawl_all.py 중복 제거)

### 1개월 (품질)
9. 테스트 프레임워크 도입 + 핵심 경로 테스트
10. API 모듈 분리 (routes, strategies, db)
11. 접근성 개선 (ARIA, 키보드 내비게이션)
12. Rate Limiting + 에러 바운더리

### 다음 분기 (성장)
13. CI/CD 파이프라인
14. 린팅 설정 (ESLint + Prettier + pylint)
15. API 문서 (OpenAPI/Swagger)
16. 상태 관리 리팩터링 (useReducer 또는 Zustand)
